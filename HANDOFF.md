# Digital Frame — מסמך העברה לצ'אט חדש

> העתק את כל המסמך הזה להודעה הראשונה בצ'אט החדש. הוא מכיל את כל מה שצריך כדי להמשיך בדיוק מאותה נקודה.

---

## מי אני ומה הפרויקט

אני טל, בעל הפרויקט והמפתח הראשי של **Digital Frame** — אתר סטודיו עברי/RTL פרימיום ב-`digital-frame-two.vercel.app`, בנוי כקובץ `index.html` יחיד עם GSAP + ScrollTrigger + Lenis. האתר מעוצב בהשראת `mdx.so` (הרפרנס).

**חלוקת התפקידים:** אני מספק צילומי מסך, נתוני DevTools, החלטות כיווניות ומשוב ברור. אתה (Claude) המפתח המבצע — עורך קוד, מריץ פעולות Git, ומדבג.

**אני מתקשר בעברית.** תשמור על אותו סגנון — עברית, ישיר, טכני.

---

## הכלל החשוב ביותר: הרפרנס הוא האמת

**כל תמונה עם טקסט באנגלית = הרפרנס (mdx.so) = התוצאה הנכונה שאליה שואפים.** אלא אם אני אומר אחרת במפורש. לפני כתיבת קוד — לחלץ את מבנה ה-DOM וה-CSS האמיתי של הרפרנס ולעבוד לפיו, לא לנחש מצילום.

הקבצים הרלוונטיים ב-`/mnt/project/`:
- `רפרנסCSS.txt` — גיליון הסגנונות האמיתי של הרפרנס (4762 שורות). **`html{font-size:0.5208vw}` פירושו 1rem=10px ברוחב 1920.** זה מפתח לכל המרת מידה.
- `אתררפרנס.txt` — ה-HTML של הרפרנס עם שמות המחלקות האמיתיים.
- צבעי המותג: `--orange:#ff6711`, `--orange-soft:#FFB878`, `--paper:#F1EFED`.

---

## היכולת הכי חשובה שגילינו: רינדור ומדידה

**יש לך Chromium זמין בקונטיינר** דרך מודול playwright:
`/home/claude/.npm-global/lib/node_modules/playwright`, מריצים עם `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node script.js`.

זה מאפשר **לרנדר את הדף ולמדוד את הפריסה בפועל** במקום להסיק אותה מקריאת קוד. זה תפס באגים שהיו בלתי נראים בקריאת CSS (למשל: אלמנטי `<nav>` שירשו `position:fixed` מכלל גלובלי). **לפני כל commit שנוגע בפריסה או ב-JS — לרנדר ולמדוד.**

**GSAP חסום ב-CDN בקונטיינר.** לכן וידאנו אותו מקומית: `npm install gsap` → `/tmp/gsap.min.js` + `/tmp/ScrollTrigger.min.js`. בבדיקות, מעתיקים את הקובץ ו-sed מחליף את כתובות ה-CDN בקבצים המקומיים. **בלי זה `gsap` הוא `undefined` וכל מסלול האנימציה מדולג — באג נסתר.**

טכניקות מדידה שהוכחו:
- **דגימת פיקסלים ב-PIL** לאימות צבעים/גיאומטריה (ערוץ כחול: כרטיס ~240, רקע-נייר ~205).
- **בדיקת hit-testing** (`document.elementFromPoint`) לגילוי אלמנטים בלתי-נראים שחוסמים קליקים.
- **הרצת `timeline.progress(0/0.5/1)` ישירות** לבדיקת מצבי אנימציה. **שים לב:** `st.scroll(st.start)` לבד **לא** מריץ את הטווין המקושר בגרסה הזו — חייבים `timeline.progress()`.

---

## תהליך העבודה (Git)

- Repo: `github.com/Talnisim01/Digital-Frame`, ענף `main`. Vercel עושה auto-deploy על push.
- Contributors: claude + Talnisim01.
- **⚠️ אבטחה:** PAT שותף בשיחה וצריך להישלל/להתחלף. בצ'אט החדש אצטרך לספק PAT חדש.
- זרימה: clone עם token ל-`/tmp/wX` → עריכה ב-python heredocs → אימות תחביר (`node --check` על סקריפטים מחולצים, איזון סוגריים ב-CSS, `pin:true` תמיד =2) → commit → push → clone טרי `--depth 1` לאימות → אני מצלם לאישור.
- **הודעות commit באנגלית**, מסבירות את השורש ולא רק את התיקון.

---

## מבנה האתר (סדר הסקשנים נעול)

hero → 3D canvas (`#about`) → ideas (`#services`) → showreel → **craft** → work/projects (`#work`) → contact → footer

> הערה: הסקשן הראשון אצל הרפרנס הוא השני אצלי — הסדר שלי מכוון ושונה מהרפרנס בכוונה. לשמור עליו.

**אזור מוגן (protected zone):** מנוע אנימציית רצף-הפריימים התלת-ממדי בסקשן ה-canvas (241 פריימי WebP, ScrollTrigger מוצמד, לוגיקת `protectedZone` להצמדה). **אסור לשבור אותו לעולם.** `protectedZone()` חייב לכסות את כל טווחי הסקשנים המוצמדים.

---

## אילוצים טכניים (לאכוף תמיד)

- לא להוסיף `pin:true` ליותר מאלמנט אחד בלי לוודא טווחי גלילה שאינם חופפים. כרגע יש **2** (הפריימים + craft).
- `protectedZone()` חייב לכסות את כל טווחי ה-pin.
- אין טיימליינים עצמאיים של GSAP מחוץ לתבנית ה-master timeline (חוץ מהתפריט, שהוא בלוק עצמאי מכוון).
- להשתמש ב-`left`/`right` פיזיים (לא `inset-inline`) לאלמנטים סביב הכדור/הספירה.
- `position:absolute` והמחלקות של ה-offset חייבים להיות על **אותו אלמנט** — offset על ילד סטטי גורם לבאגי stacking.
- `filter:blur()` בתוך `overflow:hidden` יוצר קצה קשה — להשתמש בגרדיאנטים רדיאליים / מסכות במקום.
- יחידות `ch` נפתרות מול ה-`font-size` של האלמנט עצמו — תמיד להצהיר `font-size` מפורש כשמשתמשים ב-`ch`.
- Lenis מוגבל ל-`pointer:fine` בלבד (התנגשות ScrollTrigger במגע מובייל).
- **RTL:** `translate` באחוזים לא מתהפך ב-RTL. למקם אלמנטים עם `inset-inline-start` ולהשאיר את ה-`translate` על ציר Y בלבד. `justify-self:end` נכון ב-LTR אבל דוחף שמאלה בעברית.
- **ירושת `nav{}`:** לאתר יש כלל גלובלי על `nav` (position:fixed, רקע, blur, direction:ltr). כל `<nav>` מקונן יורש אותו — לאפס במפורש position/offsets/z-index/direction/padding/background/backdrop-filter/border/shadow.

---

## מה הושלם (עבודה אחרונה, לפי סדר)

**התפריט (המבורגר overlay)** — הושלם כמעט לגמרי:
- `.menu` = מכולה שקופה (`pointer-events:none`, padding); הכרטיס הוא `.menu__wrapper` (1820×835 ברוחב 1920, לבן ~66% + blur, פינות).
- בלוק "התחברות" בצד אחד, טורי ניווט בצד השני. פריסה = **שני טורים מפורשים** (לא grid של 12 — נכשל ב-RTL), עם `min-width:0` על ילדי flex.
- **לשונית מוגבהת (שזלונג)** בפינה שבה יושב ה-X: נבנית ב-JS כ-`clip-path:path()`. המפתח: **דגל sweep=0** בקשת הקעורה (עם 1 היא מתנפחת החוצה ונוצרת גבעה). שתי הקשתות נפגשות משיקית (גובה הלשונית ≥ סכום הרדיוסים). מרכז הלשונית = מרכז שורת הניווט (y=45) כך שה-X מחליף את ההמבורגר במקומו.
- ה-X: 64px תיבה / 42px אייקון / 34 במובייל, ממורכז ב-`place-items`.
- "בואו נדבר": 20px טקסט, 8px מרווח, חץ 22×22 (מידות הרפרנס). ההמבורגר: הקווים מתכווצים (`scaleX(0)`) והמכולה נשארת, כך ש"בואו נדבר" לא זז.
- אנימציית כניסה: התחברות נכנסת מצד אחד, ניווט מהצד השני (converging), stagger שמתעצם. `0.62s`, stagger `0.042` — **ניחוש שלא אומת מול הרפרנס, ממתין למשוב על הקצב.**
- החימום המוקדם (`is-warm` ב-pointerenter) — היזהר: הוא הפך את השכבה לגלויה וחסם את הקליק. תוקן עם `pointer-events:none` על הכרטיס וה-X כשלא פתוח.

**הפוטר** — שחור (`#000`), wordmark ענק (`DIGITAL FRAME`, flex space-between, 9vw), שורת תחתית עם `grid-template-areas:"stack"` (ממרכז את הקרדיט מול העמוד). כפתור scroll-top בשם `.foot-backtop` (התנגשות שם עם `.foot-top` שברה את הרשת).

**הקונטקט** — מבנה `lets-talk` מהרפרנס. **לקח קריטי:** הרפרנס נותן כרטיס לבן ב-25% על רקע `#fff`, מה שיוצא בלתי-נראה על הנייר שלנו `#F1EFED` (הפרש 3-5/255). הרקע של הסקשן הועלה ל-`#FCFCFC` והכרטיס מקבל את ערכי הרפרנס. הטבעת חייבת להיות **בהירה** (כחלחלה 8%) — טבעת כהה על רקע בהיר נקראת כקו/סטרוק.

**המחוון (5 פסים)** — נעלם (`is-gone`) כשהפרויקטים מכסים >55% מהמסך.

**מצב הניווט הכהה (`on-dark`)** — בודק גם `.work` וגם `.foot`.

---

## הסקשן שעליו עובדים עכשיו: craft

זהו הסקשן הפעיל. יש בו שני מצבים ומעבר ביניהם:

**המבנה:**
- `.craft-video` במרכז — סרטון שמחליף את הכדור/גיף הישן.
- `.craft-flex` — הכותרת העליונה ("נבנה בכוונה. נועד להרגיש").
- `.craft-copy` — הקופי המרכזי ("אנחנו בונים חוויות").
- 4 × `.craft-pill` — הגלולות (UI/UX, פיתוח/Development, מיתוג/Branding, אוטומציה/3D Animation).

**המעבר (עובד, מאומת):**
- מצב 0 (בהגעה, לפני גלילה): הגלולות **אסופות** על הסרטון (מרוכזות ב-CSS), הכותרת העליונה גלויה, הקופי המרכזי שקוף.
- גלילה אחת קצרה (`end:'+=60%'`, סיבוב גלגלת אחד): הגלולות **מתפזרות** לפינות, הכותרת יוצאת, הקופי המרכזי נכנס.
- הסקשן **מוצמד** (pin) לאורך המעבר. `scrub:.6`.
- **מנגנון:** GSAP מזיז `x/y` על ה-`.craft-pill`; הריחוף העדין המתמשך רץ על `.craft-pill__float` (עטיפה פנימית) כדי שלא יילחם בתזוזת המעבר. (ניסיון קודם עם משתני CSS `--gx/--fx` נכשל — GSAP לא מנפיש אותם חלק.)
- מיקומי הפיזור = ה-`translate` מ-CSS הרפרנס: UI/UX `-40/-18rem`, Development `17/-16`, Branding `-40/12`, 3D Animation `21/9`, מוקנה לגודל החלון.

**הגלולות (נבנו מחדש מהמפרט המדויק של הרפרנס — `62eea1c`):**
- רקע לבן ~62% + `backdrop-filter:blur(8.5px)` (שקוף, לא לבן מלא).
- **הצבע בקצוות = `::before` עם מסגרת 1px**: גרדיאנט כתום שזוהר בשמאל וימין ודוהה לכלום באמצע, נחשף דרך `mask-composite:exclude`. **לא** מסגרת מלאה.
- מידות: padding `2.3/3.4rem`, radius `6.8rem`, gap `1.5rem`, טקסט `3rem`=30px, אייקון `2.4rem`=24px.
- אייקונים = ה-SVG המדויקים של הרפרנס (מהמבנה שטל סיפק).

**הבועות הכתומות** — שתי בריכות רדיאליות רכות בצבע כתום שנכנסות מהצדדים ברקע הסקשן (עדין, `242,234,228` מול `241,239,237` במרכז).

---

## מה פתוח / הצעד הבא

1. **⏳ הסרטון החדש (ממתין לטל).** הסרטון הנוכחי (`assets/craft-video.mp4` + `.webm`) בעייתי — יש לו שני פריימים עם רקע כהה שלא יוצאים טוב. **טל יוצר סרטון חדש ויעלה אותו.** כשמגיע: לשים ב-`assets/` באותם שמות (`craft-video.mp4` / `craft-video.webm`) והוא יתחבר אוטומטית, או להעלות לצ'אט ו-Claude ימיר ויחבר. הסרטון ממוקם ב-`.craft-video__el`: `object-fit:contain`, רוחב `min(112vh,82vw,1180px)`, `translateY(7%)` (יורד נמוך), עם מסכת `radial-gradient` להמסת הקצוות.

2. **הגלולות — לאישור/כוונון.** נבנו מחדש מהמפרט המדויק (`62eea1c`). טל צריך לאשר שהן נראות נכון עכשיו, או לציין ספציפית מה סוטה (צל/צבע/גודל/מיקום) לתיקון פרט-פרט מול ערכי הרפרנס.

3. **קצב אנימציית כניסת התפריט** — `0.62s` / stagger `0.042`, ניחוש שלא אומת. ממתין למשוב על התחושה.

4. **בעיית deployment אפשרית:** ייתכן ש-Vercel מגיש deployment ישן בגלל שני פרויקטי Vercel נפרדים (`digital-frame` מול `digital-frame-two`) שמצביעים לאותו repo, כשרק אחד מקבל עדכונים. כדאי לבדוק ולפתור.

---

## commits אחרונים (לאוריינטציה)

- `62eea1c` — בניית הגלולות מחדש מהמפרט המדויק של הרפרנס (רקע שקוף+blur, `::before` גרדיאנט קצוות, אייקונים מדויקים, מידות rem).
- `6291d05` — הגדלת הסרטון (`contain` במקום `cover` שחתך אותו) ותיקון גודל הגלולות.
- `8fdb7d0` — מרכוז הגלולות כך שהן נאספות לפני שהן מתפזרות.
- `c9b8bfe` — בניית סקשן craft מחדש: סרטון, מעבר הפוך, גלולות מרחפות, בועות.
- `3006925` — איפוס דליפת רקע הניווט, שינוי גודל כפתור סגירה במובייל.

---

## סגנון עבודה של טל (חשוב)

- משוב ישיר וברור ("עדיין הצדדים", "לא רואים בכלל", "נראה נורא ואיום"). לא להיעלב — זה מדויק ומועיל.
- מעדיף שינויים מבוססי-מדידה על ניחוש איטרטיבי.
- דוחף בחזרה כשמשהו משתנה יותר מדי בבת אחת, או כשתיקון מטפל בסימפטום ולא בשורש.
- דורש שמה שעובד יישמר כשמתקנים מה ששבור.
- **סבב אחד לכל שינוי** — לא לשנות כמה דברים בו-זמנית, כדי לבודד רגרסיות.
- מספק לפעמים קוד ישיר מהרפרנס (DOM dumps, ערכי CSS) — **להשתמש בו, לא לנחש סביבו.** זה היה מקור לכמה כשלים כשניחשתי במקום להשתמש במה שסופק.
